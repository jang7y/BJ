#include <iostream>

using namespace std;

int sol = 0;
int T = 0;
int a, b;
int x, y;
int jun = 0;
int main()
{
	x = y = 0;
	scanf("%d %d %d", &T, &a, &b);

	T = pow(2, T);
	while (x!=a && y!=b)
	{
		if (x > (T/2)-1)
			sol += 
	}

}