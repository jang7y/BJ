#include <iostream>
#include <vector>

using namespace std;

int T;
int main()
{
	vector<int>result;
	vector<int> arr;
	int money;
	int a;
	int temp=0;
	scanf("%d", &T);
	for (int i = 0; i < T; i++)
	{
		scanf("%d", &a);
		for (int j = 0; j < a; j++)
		{
			scanf("%d", &temp);
			arr.push_back(temp);
			result.push_back(temp);
		}
		temp = 0;
		scanf("%d", &money);

	

	}

}