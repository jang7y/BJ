#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;


vector<long long> arr;
int N, K;
long long temp;
long long 
int main()
{
	scanf("%d %d", &N, &K);
	for (int i = 0; i < N; i++)
	{
		scanf("%d", &temp);
		arr.push_back(temp);
	}
	for (int i = 1; i < *min_element(arr.begin(), arr.end()); i++)
	{

	}

}